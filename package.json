{
  "name": "money-trakr",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "private": true,
  "sideEffects": false,
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "dev": "dotenv -- turbo run dev"
  },
  "prettier": {},
  "eslintIgnore": [
    "/node_modules",
    "/build",
    "/public/build"
  ],
  "dependencies": {},
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
